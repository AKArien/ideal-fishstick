
export const default TipsView = ({forId}) => {
	const { forId } = props
	const [tips, setTips] = useState(null)
	const [loading, setLoading] = useState(true)
	const [error, setError] = useState(null)

	useEffect(() => {
		// fetch tips from firebase
	})


}
